{
  "dependencies": {
    "nan": "^2.15.0"
  },
  "name": "tree-sitter-clingo",
  "version": "1.0.0",
  "description": "tree-sitter parser for the clingo language",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "tree-sitter-cli": "^0.20.0"
  }
}
